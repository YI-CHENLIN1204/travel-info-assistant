services:
  api:
    network_mode: host
    ports: !reset []
    environment:
      ConnectionStrings__Postgres: "Host=127.0.0.1;Port=${POSTGRES_PORT:-5432};Database=${POSTGRES_DB:-travel_info};Username=${POSTGRES_USER:-travel_app};Password=${POSTGRES_PASSWORD:-local-development-only}"
      Redis__ConnectionString: "127.0.0.1:${REDIS_PORT:-6379}"

  web:
    network_mode: host
    ports: !reset []
    command:
      - /bin/sh
      - -c
      - |
        set -e
        sed -i 's|listen 80;|listen ${WEB_PORT:-5173};|' /etc/nginx/conf.d/default.conf
        sed -i 's|http://api:8080|http://127.0.0.1:${API_PORT:-8080}|' /etc/nginx/conf.d/default.conf
        exec nginx -g 'daemon off;'
